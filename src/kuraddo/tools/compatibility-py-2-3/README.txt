Compatibility library
=====================

The original library is called Six. It provides utility functions for smoothing over
the differences between the Python versions with the goal of writing Python code that
is compatible on both Python versions.